/* Placeholder for Scene Picker (14 / 15 / 16). See the note in ../page.tsx. */
export default function PickerPage() {
  return (
    <main className="flex flex-1 flex-col gap-2 px-10 py-8">
      <h1 className="text-[28px] font-semibold tracking-tight">Scene Picker</h1>
      <p className="text-text-secondary">Coming in the Scene Picker epic.</p>
    </main>
  );
}
