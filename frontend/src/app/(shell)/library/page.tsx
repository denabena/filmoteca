/* Placeholder for the library (06 / 12 / 13). See the note in ../page.tsx. */
export default function LibraryPage() {
  return (
    <main className="flex flex-1 flex-col gap-2 px-10 py-8">
      <h1 className="text-[28px] font-semibold tracking-tight">Library</h1>
      <p className="text-text-secondary">Coming in the Library browsing epic.</p>
    </main>
  );
}
